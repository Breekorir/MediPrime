<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MediPrime - Your Trusted Medicine Platform</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #f4f9ff;
      color: #333;
    }

    header {
      background: #0077cc;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: white;
    }

    .logo {
      font-size: 2rem;
      font-weight: bold;
    }

    .nav-links ul { /* Target the ul inside nav-links for general styling */
      list-style: none; /* Remove bullet points */
      margin: 0;
      padding: 0;
      display: flex; /* Display horizontally on larger screens */
    }

    .nav-links ul li {
      margin-left: 20px;
    }

    .nav-links ul li a { /* Corrected selector for anchor tags */
      color: white;
      text-decoration: none;
      font-weight: 500;
    }

    #menu-toggle {
      display: none;
      background: none;
      border: none;
      font-size: 1.8rem;
      color: white;
      cursor: pointer;
    }
    .hero {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      padding: 60px 40px;
      background: #e3f2fd;
    }

    .hero-text {
      flex: 1;
      padding-right: 30px;
    }

    .hero-text h1 {
      font-size: 3rem;
      color: #004c99;
    }

    .hero-text p {
      font-size: 1.2rem;
      margin: 20px 0;
    }

    .hero-text a {
      background: #0077cc;
      color: white;
      padding: 12px 24px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
    }

    .hero-image {
      flex: 1;
      text-align: center;
    }

    .hero-image img {
      width: 90%;
      max-width: 450px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .section {
      padding: 60px 40px;
      background: white;
    }

    .section h2 {
      color: #004c99;
      margin-bottom: 20px;
    }

    .features {
      display: flex;
      flex-wrap: wrap;
      gap: 30px;
    }

    .feature {
      flex: 1 1 280px;
      background: #f1f9ff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .feature h3 {
      color: #0077cc;
      margin-bottom: 10px;
    }

    .cta {
      text-align: center;
      padding: 60px 40px;
      background: #0077cc;
      color: white;
    }

    .cta h2 {
      font-size: 2.5rem;
      margin-bottom: 20px;
    }

    .cta a {
      background: white;
      color: #0077cc;
      padding: 12px 30px;
      border-radius: 6px;
      font-weight: bold;
      text-decoration: none;
    }

    footer {
      text-align: center;
      background: #002244;
      color: white;
      padding: 20px;
    }

    @media (max-width: 768px) {
      /* Hide the ul by default on small screens */
      .nav-links ul {
        flex-direction: column;
        background: #2e3192;
        position: absolute;
        top: 100px;
        left: 0;
        width: 100%; /* Make it full width */
        height: auto; /* Adjust height dynamically */
        display: none; /* Hide by default */
        text-align: center;
        padding: 20px 0; /* Add some padding */
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        z-index: 1000; /* Ensure it's above other content */
      }

      /* When the 'show' class is added to the ul, display it */
      .nav-links ul.show {
        display: flex; /* Or 'block' if you prefer */
      }

      .nav-links ul li {
        margin: 10px 0; /* Adjust vertical margin for mobile links */
      }

      #menu-toggle {
        display: block;
        margin-left: auto;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">🩺 MediPrime</div>
    <button id="menu-toggle">☰</button>

    <nav class="nav-links" id="nav-links">
      <ul> <li><a href="/">Home</a></li>
        <li><a href="/findMedicine">Find Medicine</a></li>
        <li><a href="/login">Login</a></li>
        <li><a href="/signup">Sign Up</a></li>
      </ul> </nav>
  </header>

  <section class="hero">
    <div class="hero-text">
      <h1>Access Reliable Medicines Quickly</h1>
      <p>MediPrime helps you locate available medicines in nearby pharmacies in real-time. No more moving around blindly. Stay informed, stay healthy.</p>
      <a href="/findMedicine">Search Medicines</a>
    </div>
    <div class="hero-image">
      <img src="/images/med.jpg" alt="Find Medicine">
    </div>
  </section>

  <section class="section">
    <h2>Why Choose MediPrime?</h2>
    <div class="features">
      <div class="feature">
        <h3>Real-Time Tracking</h3>
        <p>Stay up to date with real-time medicine availability from trusted pharmacies.</p>
      </div>
      <div class="feature">
        <h3>Verified Pharmacies</h3>
        <p>All listings are from verified pharmacies, ensuring safety and reliability.</p>
      </div>
      <div class="feature">
        <h3>Smart Search</h3>
        <p>Search by medicine name, location, or category and filter results easily.</p>
      </div>
      <div class="feature">
        <h3>Order Support</h3>
        <p>Logged-in users can place medicine orders directly with pharmacies.</p>
      </div>
    </div>
  </section>

  <section class="cta">
    <h2>Join MediPrime Today</h2>
    <p>Take control of your health by accessing medicine information the smart way.</p>
    <a href="/signup">Get Started</a>
  </section>

  <footer>
    &copy; 2025 MediPrime. All rights reserved.
  </footer>
  <script>
    const toggleBtn = document.getElementById('menu-toggle');
    // We now want to select the UL element inside the nav, not the nav itself
    const navUl = document.querySelector('#nav-links ul'); // Select the ul inside #nav-links

    toggleBtn.addEventListener('click', () => {
      navUl.classList.toggle('show'); // Toggle the class on the ul
    });
  </script>
</body>
</html>